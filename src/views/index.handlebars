<div class="container">
    <div class="row">
        <div class="col">
            <ul>
                {{#each products}}
                    <li>
                        {{this.title}} - {{this.stock}} - {{this.price}}
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>